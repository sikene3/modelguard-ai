# Project defaults. Change reasoning effort per phase with /model or CLI -c overrides.
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
